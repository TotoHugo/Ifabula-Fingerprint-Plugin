android {
    defaultConfig {
        ndk {
            abiFilters "armeabi-v7a", "x86"
        }

        packagingOptions {
            exclude "lib/arm64-v8a/libsqlc-native-driver.so"
            exclude "lib/x86_64/libsqlc-native-driver.so"
        }
    }
}